kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  kubeadmConfigPatches:
  - "kind: ClusterConfiguration\napiServer: \n  certSANs:\n    - \"192.168.1.42\"  # Your desired IP address\n"
  extraPortMappings:
  - containerPort: 6443 # Kubernetes API server
    hostPort: 6443
    protocol: TCP
